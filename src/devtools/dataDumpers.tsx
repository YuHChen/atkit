import Wip from "../Wip";

const CardsDataDumper: React.FC = () => (
  <Wip className="mt-3" pageName="cards data dumper page" />
);

const CombosDataDumper: React.FC = () => (
  <Wip className="mt-3" pageName="combos data dumper page" />
);

export { CardsDataDumper, CombosDataDumper };
